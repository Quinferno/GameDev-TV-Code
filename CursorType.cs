namespace RPG.Control
{
    public enum CursorType//try not to add items anywhere but end of the list, as it will mess up certain settings on prefabs potentially
    {
        None,
        Movement,
        Combat,
        Interact,
        Dead,
        UI,
        Pickup,
        FullPickup,
    }

}